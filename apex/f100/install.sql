-- ============================================================
-- PLACEHOLDER: Replace this file with your actual APEX export.
-- ============================================================
--
-- Generate the real export from SQLcl against your dev workspace:
--
--     sqlcl <user>/<pwd>@<service>
--     SQL> apex export -applicationid 100 -dir apex/f100 -split
--
-- The export produces install.sql plus an application/ subdirectory
-- with one file per page, shared component, and other metadata.
--
-- The build pipeline's "Validate APEX export integrity" step looks
-- for the wwv_flow_imp.import_begin marker below, so do not delete
-- this placeholder until you have a real export in place.
-- ============================================================

-- wwv_flow_imp.import_begin (marker only; replace this file)
PROMPT This is a placeholder APEX install script. Replace before deploying.

EXIT;
